services:
  - type: web
    name: pdfly-converter
    runtime: python
    buildCommand: "bash build.sh"
    startCommand: "gunicorn app:app"
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.0
    plan: free
